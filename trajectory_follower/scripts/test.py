import math , numpy as np
        # fig, ax = plt.subplots()
        # x_data, y_data = [], []
        # line, = ax.plot([], [], 'bo', ms = 3)
        # line2, = ax.plot([], [], 'r-')
        # line3, = ax.plot([], [], 'go')
        # ax.set_xlim(self.x_min, self.x_max)  
        # ax.set_ylim(self.y_min, self.y_max) 
        
        # x_curve, y_curve = [self.x_min], [self.y_min]
        # for i in range(2000):
        #     x_curve.append(x_curve[-1]+0.01)
        #     y_curve.append(self.f(x_curve[-1]))
        # line2.set_data(x_curve, y_curve)
                     # print(dist, t)
            # x_data.append(x_final)
            # y_data.append(self.f(x_final))
            # line.set_data(x_final, self.f(x_final))
            # plt.pause(0.000001)
        
        # xp = [x0]
        # yp = [y0]
        # line3.set_data(xp, yp)
        # plt.show()
        
        
n = np.zeros(10)
n = n[:-1]
print(n.size)